import { FC } from "react"
import { Box } from "@mui/material"

const Profile: FC = () => {
  return (
    <Box
      sx={{
        height: "100%",
      }}
    >
      PROFILE
    </Box>
  )
}

export default Profile
